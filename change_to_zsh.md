# Change shell
### Start use zsh
``` zsh  ```

### Change to zsh
``` $ chsh -s $(which zsh) ```
### Change back
``` $ chsh -s $(which bash) ```

# Ref : https://fedoramagazine.org/set-zsh-fedora-system/
